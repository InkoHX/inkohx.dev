---
import constants from '../constants'
---

<script is:inline async src={`https://www.googletagmanager.com/gtag/js?id=${constants.analytics.id}`}></script>
<script is:inline define:vars={{ id: constants.analytics.id }}>
  window.dataLayer = window.dataLayer || []
  function gtag(...args) {
    dataLayer.push(args)
  }
  gtag('js', new Date())
  gtag('config', id)
</script>
